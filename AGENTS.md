# Project Instructions & Constraints

- **Do NOT touch, edit, overwrite, replace, or download photo/image files** (e.g., in `/images/` or any binary image assets) unless explicitly instructed by the user. Modifying binary photo assets causes corruption issues during `git pull`.
